# =============================================================================
# Docker 构建排除规则
# =============================================================================
# 本文件定义了哪些文件和目录不会被包含在 Docker 构建上下文中。
# 合理配置可以加速构建过程并减小镜像体积。
# =============================================================================

# ── 版本控制 ──────────────────────────────────────────────────────────────────
.git
.gitignore
.gitattributes

# ── Docker 相关文件（避免递归）─────────────────────────────────────────────────
Dockerfile
docker-compose*.yml
.dockerignore

# ── 环境变量（绝不应包含在镜像中）─────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Node.js ───────────────────────────────────────────────────────────────────
**/node_modules
**/npm-debug.log*
**/yarn-debug.log*
**/yarn-error.log*
**/.npm
**/.yarn

# ── 构建产物（由多阶段构建重新生成）──────────────────────────────────────────
**/build
**/dist
**/.cache

# ── 测试和文档 ───────────────────────────────────────────────────────────────
**/__tests__
**/*.test.js
**/*.test.ts
**/*.spec.js
**/*.spec.ts
**/coverage
**/*.md
!**/README.md

# ── IDE 和编辑器 ─────────────────────────────────────────────────────────────
.vscode
.idea
**/.swp
**/.swo
**/*~
**/*.sublime-*

# ── 操作系统文件 ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
**/.DS_Store

# ── 日志和临时文件 ───────────────────────────────────────────────────────────
logs/
**/*.log
tmp/
temp/

# ── 备份文件 ─────────────────────────────────────────────────────────────────
backups/
**/*.bak
**/*.sql.gz

# ── SSL 证书（绝不应包含在镜像中）────────────────────────────────────────────
nginx/ssl/
**/*.pem
**/*.key
**/*.crt

# ── 脚本（运行时通过挂载使用，不需要打包进镜像）──────────────────────────────
scripts/

# ── 其他 ─────────────────────────────────────────────────────────────────────
**/.env.local
**/.env.*.local
docker-tutorial/
