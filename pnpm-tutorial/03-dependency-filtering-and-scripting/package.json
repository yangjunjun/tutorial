{
  "name": "03-filtering-and-scripting-root",
  "version": "1.0.0",
  "private": true,
  "description": "pnpm 多包脚本过滤与并发执行演示项目的根目录",
  "scripts": {
    "build-all": "pnpm -r run build",
    "build-parallel": "pnpm -r --parallel run build"
  }
}
